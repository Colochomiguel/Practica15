<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Navegacion.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="60"
            Padding="20,0"
            VerticalOptions="Center"
            BackgroundColor="LightSalmon">

            <Image
                Source="robot.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="250"
                HorizontalOptions="Center" />

            
            <Button BackgroundColor="White" Text="App Cambio" x:Name="Cambio" Clicked="Cambio_Clicked" HorizontalOptions="Center" />
            <Button BackgroundColor="White" Text="App Dino" x:Name="Dino" Clicked="Dino_Clicked" HorizontalOptions="Center"/>
            <Button BackgroundColor="White" Text="App Gas" x:Name="Gas" Clicked="Gas_Clicked" HorizontalOptions="Center" />
            <Button BackgroundColor="White" Text="App Superficie" x:Name="Superficie" Clicked="Superficie_Clicked" HorizontalOptions="Center"/>
            <Button BackgroundColor="White" Text="Acerca de nosotros" x:Name="Acerca" Clicked="Acerca_Clicked" HorizontalOptions="Center"/>
            
            

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
